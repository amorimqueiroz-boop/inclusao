# Copie este arquivo para secrets.toml e preencha com suas credenciais do Supabase.
# Nunca commite secrets.toml no Git (ele deve estar no .gitignore).

# URL do seu projeto Supabase (ex: https://xxxxx.supabase.co)
SUPABASE_URL = "https://SEU_PROJETO.supabase.co"

# Chave anônima (anon/public) — Project Settings > API no Supabase (login e RPC)
SUPABASE_ANON_KEY = "sua-chave-anon-aqui"

# Opcional: chave service_role (Project Settings > API) — acesso total aos dados, ignora RLS.
# Use se os dados salvos não aparecerem (ex.: tabelas students, peis com RLS ativo).
# SUPABASE_SERVICE_ROLE_KEY = "sua-chave-service-role-aqui"

# Opcional: modo teste (local) — mostra menu do Streamlit e PULA a tela de login
# ENV = "TESTE"
